import { BlendShell } from "@/components/blend/blend-shell";

export const metadata = {
  title: "Blend — two listeners, one feed",
  description:
    "Add someone by email and get a shared feed, with every story labelled by whose taste it came from.",
};

export default function BlendPage() {
  return <BlendShell />;
}
